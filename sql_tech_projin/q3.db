-- 各国の平均身長を高い方から順に表示してください。ただし、FROM句はcountriesテーブルとしてください

SELECT
  c.name AS country,
  AVG(p.height) AS avgHeight
FROM
  countries c
JOIN
  players p
ON
  c.id = p.country_id
GROUP BY
  c.id, c.name
ORDER BY
  AVG(p.height) DESC;
